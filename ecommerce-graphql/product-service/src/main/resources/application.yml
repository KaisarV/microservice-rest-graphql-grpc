server:
    port: 8081


spring:
  graphql:
    graphiql:
      enabled: true

  websocket:
    path: /graphql

  application:
    name: product-rest
  data:
    mongodb:
      host: localhost
      port: 27017
      database: product-rest

